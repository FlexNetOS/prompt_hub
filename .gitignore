# Rust build artifacts
/target

# Local databases created by the CLI / tests
*.db
*.db-shm
*.db-wal
prompthub.db

# Editor / OS
*.rs.bk
.DS_Store
.worktrees/

# .handoff ledger residency (ADR-0004 §3: ledger is local; fleet/witnessed state is the kernel's)
.handoff/ledger.db
.handoff/*.db
